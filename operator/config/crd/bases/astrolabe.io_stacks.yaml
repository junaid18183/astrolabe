---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: stacks.astrolabe.io
spec:
  group: astrolabe.io
  names:
    kind: Stack
    listKind: StackList
    plural: stacks
    singular: stack
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: StackSpec defines the desired state of Stack
            properties:
              backendRef:
                description: StackBackendRef matches backendRef in stack.yaml
                properties:
                  name:
                    type: string
                required:
                - name
                type: object
              credentialRef:
                description: StackCredentialRef matches CredentialRef in stack.yaml
                properties:
                  name:
                    type: string
                required:
                - name
                type: object
              modules:
                items:
                  properties:
                    dependsOn:
                      items:
                        type: string
                      type: array
                    name:
                      type: string
                    variables:
                      x-kubernetes-preserve-unknown-fields: true
                  required:
                  - name
                  type: object
                type: array
            required:
            - backendRef
            - credentialRef
            - modules
            type: object
          status:
            description: StackStatus defines the observed state of Stack
            properties:
              events:
                items:
                  properties:
                    message:
                      type: string
                    reason:
                      type: string
                    timestamp:
                      type: string
                    type:
                      type: string
                  required:
                  - type
                  type: object
                type: array
              logs:
                type: string
              outputs:
                x-kubernetes-preserve-unknown-fields: true
              phase:
                type: string
              resources:
                items:
                  properties:
                    id:
                      type: string
                    name:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  required:
                  - id
                  - name
                  - status
                  - type
                  type: object
                type: array
              status:
                type: string
              summary:
                type: string
            type: object
        type: object
    served: true
    storage: true
